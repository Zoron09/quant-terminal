# VERIFICATION BAR FOR ANY CHANGE TO THIS FILE: BOOT THE APP, DON'T JUST IMPORT.
# Create a fresh venv, `pip install -r requirements.txt` into it, then actually
# START the server against it and hit a real endpoint. An import-only smoke test
# is NOT sufficient and has already passed this file twice while the app could not
# start at all (2026-08-07): FastAPI raises the missing-python-multipart error from
# inside the @app.post("/api/scan") decorator at import time, so nothing surfaces
# until the app is genuinely run. The local .venv also hides gaps of this shape,
# because packages removed from this file are never uninstalled from it.

# Web stack. Pinned to exactly what the app was developed and verified against —
# these were previously UNDECLARED despite being the two hard runtime deps
# (api/server.py and run.py). Nothing installed them on purpose; they arrived as
# transitive deps and the app happened to work.
fastapi==0.138.0
uvicorn==0.49.0

# FastAPI needs this to parse multipart form data, which /api/scan requires for
# its `file: UploadFile = File(...)` CSV upload. WITHOUT IT THE APP DOES NOT BOOT —
# FastAPI raises "Form data requires python-multipart to be installed" while
# building the route at import time, before the server ever listens.
# It was only ever present as a TRANSITIVE dep of streamlit, so removing streamlit
# in bdf1e71 (correct on its own terms — zero imports) silently took this with it.
# The gap stayed invisible because the local .venv still has streamlit installed.
python-multipart==0.0.32

yfinance>=0.2.31
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0
requests>=2.31.0
python-dotenv>=1.0.0

# KEPT DELIBERATELY, NOT VERIFIED AS USED (2026-08-01 audit).
# Neither is imported anywhere in the live codebase (zero hits across all .py
# files outside .venv). Both are retained because the intent to use them is
# documented on disk rather than in code, and removing a trading dependency on
# grep evidence alone is the wrong risk trade:
#   - alpaca-py: `.env` carries ALPACA_API_KEY and ALPACA_API_SECRET. No code
#     reads either key yet, so this is a wired-but-unimplemented integration.
#   - PyPortfolioOpt: RENAMED, not removed. The line read `pypfopt>=1.5.5`,
#     which is the IMPORT name, not a PyPI distribution. Proven broken by a
#     clean-venv install on 2026-08-01: "No matching distribution found for
#     pypfopt>=1.5.5 (from versions: none)" — pip aborted the whole file, so
#     `pip install -r requirements.txt` has been failing outright on any fresh
#     machine. The installed distribution is PyPortfolioOpt (1.6.0 in .venv),
#     which is what provides `import pypfopt`. Version floor left unchanged.
alpaca-py>=0.20.0
PyPortfolioOpt>=1.5.5

# Unofficial, reverse-engineered Wealthsimple API client (not an official SDK) — used by tools/wealthsimple_export.py
ws-api

# Code 33 engine dependencies. The engine itself was vendored into code33/ on
# 2026-08-01 (commit 6107e77), replacing the editable code33-screener install
# that previously declared these — without these lines they would be undeclared.
# PINNED, not floated: the engine's correctness is bound to secfsdstools'
# behaviour (its parallel-download setting caused the historic 32% silent
# quarter-loss bug), and all validation to date was done against exactly these
# two versions.
secfsdstools==2.4.3
edgartools==5.39.1

# News Scanner (api/news_scanner.py). Both were REMOVED from this file on
# 2026-08-01 after an audit found zero imports anywhere in the codebase — that
# was correct at the time. Stage 1 of the News Scanner now genuinely imports
# both, so they are declared again rather than left as undeclared runtime deps:
#   - finnhub-python: free-tier general market + company news
#   - feedparser: SEC EDGAR 8-K current-events atom feed
finnhub-python>=2.4.28
feedparser>=6.0.12

# /api/news sources 1 and 2 (api/server.py). Both were UNDECLARED runtime deps —
# present in the local .venv, absent from this file — so a clean install degraded
# the endpoint to its yfinance fallback alone while still returning HTTP 200.
# Confirmed 2026-08-07 in a fresh venv: "[news] SeekingAlpha failed: No module
# named 'FinNews'" and "[news] tradingview-scraper failed: No module named
# 'tradingview_scraper'". Both imports sit inside try/except blocks that only
# print, so the loss is silent — nothing errors, the feed is just quietly thinner.
# Pinned to the versions the running environment actually has.
FinNews==1.1.0
tradingview-scraper==0.4.20

# Ceiling, not a floor, and NOT cosmetic: both packages above do a bare
# `import pkg_resources` (FinNews/source_object.py:4), and setuptools DELETED
# pkg_resources in 81.0.0. A fresh install pulls setuptools 83.x, so without this
# both news sources raise ModuleNotFoundError and /api/news silently falls back to
# yfinance alone — the exact failure the two lines above exist to fix, just with a
# different error message. Found 2026-08-07 only by booting the app: declaring the
# packages was not enough to make them importable.
# The running .venv has 80.10.2, which predates the removal, which is why this was
# invisible locally. Revisit if either package drops its pkg_resources use.
setuptools<81
